import React from 'react';
import { Loader } from '../components/UI';

const AuthEmployee = () => {
    return (
        <div>
            <Loader />
        </div>
    );
};

export default AuthEmployee;
